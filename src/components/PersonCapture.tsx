import * as React from 'react';
import { Person } from './PersonType';
import {
    Grid,
    TextField,
    Select,
    MenuItem,
    InputLabel
} from '@material-ui/core';
import { useParams } from 'react-router-dom';


type RoutingParams = 'id';

export interface ICapturePersonProps { };

export function PersonCapture(props: ICapturePersonProps) {
    
}